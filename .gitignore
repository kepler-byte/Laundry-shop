# =========================
# 1. Dependencies (โฟลเดอร์ Library)
# =========================
# ห้ามเอาขึ้นเด็ดขาด เพราะไฟล์เยอะและหนักมาก ทุกคนสามารถโหลดใหม่ได้ผ่านคำสั่ง npm install
node_modules/

# =========================
# 2. Environment Variables (ความลับของระบบ)
# =========================
# ใช้เก็บ URL ฐานข้อมูลของ Prisma หรือ API Key ห้ามให้ใครเห็นเด็ดขาด
.env
.env.*
!.env.example # อนุญาตให้เอาไฟล์ตัวอย่างขึ้นไปได้ (ถ้าสร้างไว้)

# =========================
# 3. Database (ฐานข้อมูล SQLite)
# =========================
# ห้ามเอาไฟล์ฐานข้อมูลจริงที่มีข้อมูลลูกค้า/ออเดอร์ร้านซักรีดขึ้น GitHub
*.db
*.db-journal
*.sqlite
*.sqlite3

# =========================
# 4. Build Outputs (ไฟล์ที่ผ่านการ Build)
# =========================
# ไฟล์ที่ระบบสร้างขึ้นมาตอนเตรียมนำไปใช้จริง (Production)
dist/
build/
.output/
.vite/

# =========================
# 5. Logs & System Files (ไฟล์ระบบและไฟล์บันทึกข้อผิดพลาด)
# =========================
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
Thumbs.db

# =========================
# 6. Editor Directories (การตั้งค่าโปรแกรมเขียนโค้ด)
# =========================
.vscode/
.idea/